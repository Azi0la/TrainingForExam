<Page x:Class="_3.Pages.SecondPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_3.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SecondPage"
      Background="Salmon">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <Label Content="Выбор Второго" FontSize="30" VerticalAlignment="Center"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BackBTN" Width="100" Height="50" Margin="10" Content="Назад" FontSize="26" Click="BackBTN_Click"/>
            <Button x:Name="SkipBTN" Width="150" Height="50" Margin="10" Content="Пропустить" FontSize="26" Click="SkipBTN_Click"/>
        </StackPanel>
        <ListBox Grid.Row="1" 
         ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
         x:Name="SecondList" d:ItemsSource="{d:SampleData ItemCount=5}"
         MouseDoubleClick="SecondList_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <Image Source="{Binding ImagePath}" Width="150" Height="150"/>
                        <TextBlock Text="{Binding Name}" FontSize="24"/>
                        <TextBlock Text="{Binding Price, StringFormat=Цена: {0}}" FontSize="24"/>
                        <TextBlock Text="{Binding Description}" FontSize="16"/>

                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel HorizontalAlignment="Stretch">

                    </WrapPanel>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</Page>
